# GL sample drops

Drop sanitized GL-side sample exports here (CSV). **Do not** include credentials.

## Needed now
- `GLDT_202501.csv` (or similar) — one month window matching the dispatch/AR sample (e.g. 2025-01).

### Requested columns (as available)
- posting date
- batch date
- batch number
- batch sequence
- journal id
- account
- debit
- credit
- amount
- reference fields / description / memo fields

If any of these fields don’t exist in your GLDT extract, include what you have and note the field names.
